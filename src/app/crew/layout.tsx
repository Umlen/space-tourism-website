function LayoutCrew({children}: {children:React.ReactNode}) {
  return (
    <section>
      <p>
        <span>02 </span>
        meet your crew
      </p>
      {children}
    </section>
  );
}

export default LayoutCrew;
